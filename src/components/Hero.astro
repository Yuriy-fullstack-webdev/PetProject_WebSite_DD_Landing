---
import { Image } from "astro:assets";
import ButtonOrder from "./elements/ButtonOrder.astro";
import LinkConsultation from "./elements/LinkConsultation.astro";
import imgCat from "../assets/images/hero/cat.png";
import imgBall from "../assets/images/ball.png";
---

<section class="hero">
  <div class="container">
    <div class="hero-box">
      <div class="hero-wrap">
        <section class="hero-info">
          <p class="hero-subtitle">Ваш бізнес заслуговує на яскравий онлайн-старт!</p>
          <h1 class="hero-title">
            <span class="text-nowrap">Des<span class="hero-title__ball ball-decor">i</span>gn</span><br />
            <span>+&nbsp;Development</span>
            <span class="ball-decor ball-decor1"></span>
          </h1>
          <div class="hero-text">
            <p>
              Ми – художники цифрового світу, які створюють полотна, що зачаровують, надихають та приносять результат.
            </p>
            <p>
              Ми пишаємося тим, що можемо допомогти нашим клієнтам досягти їхніх цілей, розкрити їхній потенціал та
              зробити їхній бізнес успішним.
            </p>
          </div>
          <div class="hero-order">
            <ButtonOrder color="purple" breakpoint="mob" />
            <LinkConsultation breakpoint="mob" />
          </div>
        </section>
        <section class="img-wrap">
          <div class="img-wrap__box">
            <Image class="img-wrap__box__image"
            src={imgCat}
            widths={[360, 435]}
              sizes={`(max-width: 756px) 360px, 435px`}
              alt="cat"
              width="435" height="435"
              quality="70"
            />
          </div>
          <div class="ball-bg ball-bg__size ball-bg__size--xs ball-bg--top0">
            <Image class="ball" src={imgBall} alt="ball" />
          </div>
        </section>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    background-color: var(--bg-primary);
    width: 100%;
    margin-top: var(--header-height);
    max-height: 900px;
    height: calc(100vh - var(--header-height));
    min-height: 600px;
    overflow: hidden;
  }

  .hero > .container {
    height: 100%;
  }
  .hero-box {
    height: inherit;
    display: flex;
    align-items: center;
  }

  .hero-wrap {
    width: 100%;
    height: 54%;
    display: flex;
    align-items: center;
    justify-content: space-between;
    column-gap: calc(var(--gap-base) * 2);
  }

  .hero-info {
    position: relative;
    z-index: 1;
    max-width: 519px;
    min-height: 30.25rem;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    row-gap: 40px;
  }

  .hero-subtitle {
    font-size: var(--font-size-20-16);
    font-weight: 500;
    color: rgba(255, 159, 28, 1);
    line-height: var(--line-height-m);
    text-wrap: balance;
  }

  .hero-title {
    width: 100%;
    height: auto;
    position: relative;
    color: var(--text-gray-100);
    font-size: clamp(2.75rem, 1.237rem + 5.263vw, 4.5rem);
    font-weight: 800;
    line-height: var(--line-height-s);
  }

  .hero-text {
    color: var(--text-gray-300);
    font-size: var(--font-size-18-14);
    font-weight: 400;
    line-height: var(--line-height-m);
    text-wrap: balance;
  }

  .hero-order {
    display: flex;
    align-items: center;
    justify-content: space-between;
    column-gap: var(--gap-base);
  }

  .hero-title__ball {
    position: relative;
  }

  .ball-decor::before {
    position: absolute;
    content: "";
    top: 0;
    left: 0;
    background-image: url("../assets/images/ball.webp");
    background-size: 100% 100%; /* Растягивает изображение по всей площади псевдоэлемента */
    background-repeat: no-repeat;
    width: clamp(0.875rem, 0.551rem + 1.128vw, 1.25rem);
    height: clamp(0.875rem, 0.551rem + 1.128vw, 1.25rem);
  }

  .ball-decor1::before {
    top: 16%;
    left: 80%;
  }

  .img-wrap {
    position: relative;
    width: 435px;
    min-width: 360px;
    height: auto;
    aspect-ratio: 1;

    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;

    box-sizing: border-box;
    border: 1px dashed rgba(239, 239, 239, 0.55);
    border-radius: 50%;
    margin-right: 42px;
  }

  .img-wrap__box {
    position: relative;
    width: 80%;
    height: auto;
    aspect-ratio: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  .img-wrap__box__image {
    /* position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%); */
    width: 128%;
    height: auto;
    object-fit: cover;
  }

  /** ball */

  .ball {
    width: 100%;
    height: 100%;
  }

  .ball-bg {
    position: absolute;
    z-index: 10;
  }

  .ball-bg--top0 {
    top: 0;
    transform-origin: 50% 217px;
    transform: rotate(210deg);
  }

  .ball-bg--center {
    top: calc(var(--size-ball) / 2 * (-1));
  }

  .ball-bg__size {
    height: auto;
    aspect-ratio: 1;
  }

  .ball-bg__size--xs {
    --size-ball: 14px;
    width: var(--size-ball);
  }

  .ball-bg__size--small {
    width: 20px;
  }

  .ball-bg__size--medium {
    width: 22px;
  }

  @media (max-width: 992px) {
    .hero {
      position: relative;
      overflow-x: hidden;
    }
    .hero-text {
      display: flex;
      flex-direction: column;
      row-gap: var(--font-size-20-16);
    }

    .img-wrap {
      position: absolute;
      z-index: 0;
      left: 45vw;
    }

    .ball-decor1::before {
      top: 83%;
      left: 7.5%;
    }
  }

  @media (max-width: 567px) {
    .hero-order {
      flex-wrap: wrap;
      row-gap: 36px;
    }

    .hero-box {
      align-items: start;
      margin-top: var(--header-height);
    }

    .hero-text {
      width: 80%;
      min-width: 244px;
    }
    .hero-order {
      margin-top: 36px;
    }
  }

  @media (max-width: 460px) {
    .hero-info {
      row-gap: 24px;
    }
  }
</style>
